accounts:
  - id: M
    githubUser: "matokle-sudo"
    enabled: true
  - id: W
    githubUser: "REPLACE_WITH_W_HANDLE"
    enabled: false
  - id: R
    githubUser: "REPLACE_WITH_R_HANDLE"
    enabled: false

scan:
  includeForks: false
  includeArchived: false
  maxFileBytes: 1000000
  redactSecrets: true
  storeExactGps: false

# Report will be emailed to this address after each scan.
# Replace with M's real email to enable sending.
contactEmail: "REPLACE_WITH_CONTACT_EMAIL"